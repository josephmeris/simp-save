import React, { Component } from 'react';
import { withRouter } from 'react-router-dom';

class UserRegisterPortal extends Component {
  render() {
    return (
        <div>
          <h2>Register</h2>
        </div>
    );
  }
}

export default withRouter(UserRegisterPortal);